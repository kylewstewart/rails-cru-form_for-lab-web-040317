<h3> All Genres </h3>

<% @genres.each do |genre| %>
  <div> Name: <%= link_to(genre.name, genre_path(genre)) %> </div>
<% end %>
<br>
<br>
<div> <%= link_to("Create a Genre", new_genre_path) %>  </div>
